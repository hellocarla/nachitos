/*const express = require('express');
const router = express.Router();

const middlewareController = require('../controllers/middlewareController');

//Autenticação
router.post('/', middlewareController.authMiddleware);

module.exports = router;
*/