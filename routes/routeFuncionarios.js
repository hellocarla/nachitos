var express = require('express');  // call express
var Funcionarios = require('../app/models/recomendacoes');
var router = express.Router(); // get an instance of the express Router
var mongoose = require('mongoose');